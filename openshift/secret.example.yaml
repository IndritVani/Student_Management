# Copy to secret.yaml and change the values before applying. Do NOT commit real secrets.
# Create quickly instead with:
#   oc create secret generic student-management-admin \
#     --from-literal=username=admin --from-literal=password='choose-a-strong-one'
apiVersion: v1
kind: Secret
metadata:
  name: student-management-admin
  labels:
    app: student-management
type: Opaque
stringData:
  username: admin
  password: change-me-please
